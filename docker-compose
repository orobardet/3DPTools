version:'2'

redis:
    restart: always
    image: redis:alpine

3dptools:
    restart: always
    images: orobardet/3dptools:latest
    environment:
        - redis__host=redis
        - database__url=mongodb://nas/3dptools
    links:
        - redis
        - mongo-db:mongo
    ports:
        - "3000:3000"
