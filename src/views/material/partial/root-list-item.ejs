<%- include('list-item.ejs', {material, isMaterialVariant: false}) %>

<% if (material.variants && material.variants.length) { %>
<% for (let variant of material.variants) { %>
<%- include('list-item.ejs', {material: variant, isMaterialVariant: true}) %>
<% } %>
<% } %>
